#!/bin/bash -eu

# Automatic backup script
#
# Link from:
# /etc/cron.weekly/

# ------------------------------------------------------------------------------
# Config (can be overriden by ENVVAR)
LOG_FILE=${LOG_FILE:-"/root/log/bkup.log"}
# ------------------------------------------------------------------------------

SELF_DIR=$(dirname "$(realpath "$0")")
ROOT=${SELF_DIR}/../backup

mkdir -p "$(dirname "${LOG_FILE}")"
"${ROOT}/bkup_full.sh" >> "${LOG_FILE}" 2>&1
